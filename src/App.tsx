import React from 'react';

// Application root component
const App: React.FC = () => {
  return (
    <main className="min-h-screen bg-white p-6">
      {/* Display the required text in red */}
      <p className="text-red-600">standard champion</p>
    </main>
  );
};

export default App;
